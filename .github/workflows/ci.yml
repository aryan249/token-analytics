name: CI/CD

on:
  push:
    branches: [main, staging, feat/*]
  pull_request:
    branches: [main, staging]

env:
  AWS_REGION: ap-south-1
  ECR_REPOSITORY: token-analytics
  IMAGE_TAG: ${{ github.sha }}

jobs:
  lint-build-test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4.1.7

      - uses: actions/setup-node@v4.0.3
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Lint
        run: npx eslint src/ --max-warnings 0 || true

      - name: Type check
        run: |
          npx tsc --noEmit 2>&1 | grep -v "node_modules/" > /tmp/tsc-out.txt || true
          if grep -q "error TS" /tmp/tsc-out.txt; then
            cat /tmp/tsc-out.txt
            exit 1
          fi

      - name: Unit tests
        run: npm test

      - name: Dependency audit
        run: npm audit --audit-level=high || true

  docker-build-push:
    needs: lint-build-test
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/staging' || startsWith(github.ref, 'refs/heads/feat/')
    runs-on: self-hosted
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4.1.7

      - name: Login to ECR
        run: aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin 207270671706.dkr.ecr.$AWS_REGION.amazonaws.com

      - name: Build Docker image
        run: |
          REGISTRY=207270671706.dkr.ecr.$AWS_REGION.amazonaws.com
          IMAGE=$REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker build -t $IMAGE .
          docker tag $IMAGE $REGISTRY/$ECR_REPOSITORY:latest
          echo "IMAGE=$IMAGE" >> $GITHUB_ENV

      - name: Scan image for vulnerabilities
        run: |
          # Install Trivy if not present
          which trivy || (curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sh -s -- -b /usr/local/bin)
          trivy image --exit-code 0 --severity HIGH,CRITICAL --no-progress $IMAGE
        continue-on-error: true

      - name: Push Docker image
        run: |
          docker push $IMAGE
          docker push $(echo $IMAGE | sed "s/:.*/:latest/")

      - name: Update Helm values with new image tag
        run: |
          REGISTRY=207270671706.dkr.ecr.$AWS_REGION.amazonaws.com
          sed -i "s|tag:.*|tag: \"$IMAGE_TAG\"|" helm/token-analytics/values.yaml
          sed -i "s|repository:.*|repository: $REGISTRY/$ECR_REPOSITORY|" helm/token-analytics/values.yaml

      - name: Commit and push image tag update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add helm/token-analytics/values.yaml
          git diff --cached --quiet && exit 0
          git commit -m "chore: update image tag to ${IMAGE_TAG:0:8}"
          # Retry push with rebase to handle concurrent CI runs
          git push || (git pull --rebase && git push)

  # ── No deploy job — Argo CD watches git and syncs automatically ────────────
  #
  # Promotion workflow:
  #   Push to staging branch → Argo CD syncs to token-analytics-staging
  #   Merge to main          → Argo CD syncs to token-analytics (production)
  #
  # Drift detection: selfHeal=true reverts manual kubectl changes within 3 min
  # Rollback: argocd app rollback token-analytics-production <revision>
